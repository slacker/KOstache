<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<title>{{title}}</title>
	</head>
	<body>
		<h1>{{title}}</h1>
		<p>Here are all my {{things}}:</p>
		{{#tests?}}
		<ul>
			{{#tests}}
			<li class="{{class}}"><strong>{{#object}}{{id}}{{/object}} (class {{class}}):</strong> ({{#object}}{{name}}:{{value}}{{/object}})</li>
			{{/tests}} 
		</ul>
		{{/tests?}}
	</body>
</html>